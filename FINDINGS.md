<div align="center">

# 🧮 Math-Expression Parser & Evaluator — Full Cross-Language Report

**The same fifteen parsing/evaluation strategies in C++, Rust, Haskell, and Python.**

</div>

> Detailed companion to the [README](README.md).

| | language | memory model |
|---|---|---|
| [`cpp/`](cpp/) | C++26 | manual / RAII, arenas |
| [`rust/`](rust/) | Rust (LLVM) | ownership, arenas; safe Rust, no `unsafe` |
| [`haskell/`](haskell/) | Haskell (GHC) | lazy, GC, persistent |
| [`python/`](python/) | Python 3 | reference-counted, everything boxed |

All four share **one grammar** and benchmark on **byte-identical corpora**
(`bench/corpus/`, generated by [`bench/gen_corpus.py`](bench/gen_corpus.py)).

## The fifteen strategies

`ast-recursive-descent`, `ast-shunting-yard`, `ast-pratt`, `ast-arena`,
`multipass` (top-down divide-and-conquer), `multipass-arena`, `multipass-bfs`,
`multipass-reverse` (bottom-up), `multipass-reverse-fold` (bottom-up, fused),
`direct-recursive-descent`, `direct-shunting-yard`, `direct-reverse`,
`direct-mp`, `bytecode-vm`, and `direct-scannerless` — recursive descent with
the lexer fused in, the control row for what the shared lexer costs (see
[Lexing rules](#lexing-rules--applied-to-every-parser)).
Full per-strategy descriptions: [`cpp/README.md`](cpp/README.md).

## Grammar

Numbers, single-letter variables `a`–`z`, `+ - * / ^`, unary `+/-`, parentheses.
Precedence: `+ -` < `* /` < unary < `^` (right-associative). So `-2^2 = -4`, `2^3^2 = 512`.

## Build & run

```sh
python3 bench/gen_corpus.py        # generate shared corpora (once)

# C++  (needs GCC 14+ / Clang 17+ for <print>)
cmake -S cpp -B build -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_COMPILER=g++-14
cmake --build build -j && ctest --test-dir build
./build/corpus_bench               # shared-corpus timing (cross-language comparable)

# Rust  (stable toolchain)
cd rust && cargo test --release && cargo run --release --bin bench

# Python  (3.10+)
python3 python/test_parsers.py && python3 python/test_fuzz.py
python3 python/bench.py

# Haskell  (GHC + cabal; needs libgmp — apt install libgmp-dev if linking fails)
cd haskell && cabal test && cabal run bench
```

Correctness is enforced two ways: curated spec suites in each language (the
same cases, including IEEE corners and literal overflow → `inf`/`0`), plus
differential fuzzing in all four languages ([C++](cpp/tests/fuzz_differential.cpp)
· [Rust](rust/tests/fuzz.rs) · [Python](python/test_fuzz.py) ·
[Haskell](haskell/app/Fuzz.hs)) — all fifteen strategies must agree, value
or rejection, on the same 3 000 random + 3 000 mutated inputs per run.

## Cross-language results

Median ns/leaf across the four sizes (n=10…10000), normalised to each
language's fastest *real* strategy (the lexer-free control is excluded from
the baseline, shown in italics against it instead) — the column shows
*ranking*, not cross-language speed. Neutral 4-vCPU GitHub runner, median
of three independent CI runs of one commit (36226353574, 36226831278,
36227295521, 2026-09-26, commit 921ec7f), all three on one CPU model (AMD
EPYC 9V74, read from each run's `env.txt`). EPYC 9V74 is the model this
repo publishes; 18 runs of the commit were dispatched and these are the
first three, in dispatch order, that landed on it — the rest (EPYC 7763
and 9V45, Xeon 8370C and 8573C, one later 9V74) are not mixed in. Ratios
do shift between models: on three EPYC 7763 runs of the same commit Rust's
fold trails `ast-arena` by 3–7 % on the random corpus and its
`direct-reverse` trails `direct-rd` by 5–7 %, where on the 9V74 both are
ties ([one-pager](docs/one-pager.md) lists every cell that flips). Toolchains are pinned
(GCC 14.2, rustc 1.98.1, GHC 9.6.7, cabal 3.10.3.0, the image's Python
3.12), and within each run the strategies are timed round-robin, so drift
on the runner falls on all of them alike. One of the three runs
(36226831278) was ~1.3× slower across the board (~1.1× in Haskell) on the
same CPU model; ratios within a run are unaffected, and the medians sit on
the two faster runs.
**Trust the tiers, not the digits** (each `1.0` marks a cluster of
near-ties, not a clear winner).

| strategy | C++ ×fastest | Rust ×fastest | Python ×fastest | Haskell ×fastest |
|---|--:|--:|--:|--:|
| ast-recursive-descent | 2.6 | 2.3 | 1.3 | **1.0** |
| ast-shunting-yard | 2.6 | 2.4 | 1.2 | 1.2 |
| ast-pratt | 2.6 | 2.2 | 1.2 | **1.0** |
| **ast-arena** | 1.4 | 1.4 | 1.4 | 1.1 |
| multipass (pointer) | 4.0 | 5.1 | 2.0 | 1.7 |
| multipass-arena | 2.6 | 3.4 | 2.1 | 1.8 |
| direct-mp | 2.1 | 3.1 | 1.9 | 1.8 |
| multipass-bfs (sparse RMQ) | 2.9 | 3.9 | 2.5 | 2.2 |
| multipass-reverse | 2.0 | 1.9 | 1.7 | 1.5 |
| **multipass-reverse-fold** | 1.4 | 1.4 | 1.2 | 1.1 |
| direct-recursive-descent | **1.0** | **1.0** | 1.1 | **1.0** |
| direct-shunting-yard | **1.0** | 1.1 | 1.1 | 1.2 |
| direct-reverse | **1.0** | **1.0** | **1.0** | 1.1 |
| bytecode-vm | 1.3 | 1.3 | 1.2 | 1.2 |
| *direct-scannerless* (lexer-free control) | *0.8* | *0.6* | *0.8* | *0.9* |

Raw fastest, median ns/leaf: **C++ ≈ 40** (`direct-sy`, with
`direct-reverse` / `direct-rd` 2–3 % behind), **Rust ≈ 47** (`direct-rd`,
with `direct-reverse` inside 0.5 %), **Haskell ≈ 529** (`ast-pratt`, with
`direct-rd` and `ast-recursive-descent` within ~1 % and `direct-reverse`
~5 % behind), **Python ≈ 2 021** (`direct-reverse`, `direct-sy` ~11 %
behind) — C++ and Rust are ~11–13× faster than Haskell and ~43–50× faster
than Python in absolute terms. The lexer-free control sits at 32 / 29 /
480 / 1 694 (C++ / Rust / Haskell / Python). Absolute numbers are not
comparable across batches — they moved ~1.3× between two 9V74 batches of
near-identical code, and within this one — so only within-run ratios are
compared. Haskell's run-to-run ratio spread fell 5–10× with the 64 MB
nursery (`-A64m`) and round-robin timing (#13), and its tiers did not
move.

### What survives the change of runtime

1. **In C++, memory layout dominates.** Everything *contiguous* — `direct-*`
   (no tree), `bytecode-vm`, `ast-arena`, the fused reducer — clusters at
   1.0–1.4; every pointer-walking form pays ~2.5× for chasing `make_unique`'d
   nodes. Rust reproduces the C++ tiers almost line for line — same
   clusters, same ratios — under a different compiler (LLVM), so the tiers
   are the memory model's, not GCC's. Where the two differ is the top-down
   family (the Rust port has no SIMD candidate scan, so `multipass-arena` /
   `direct-mp` sit at 3.1–3.4× instead of 2.1–2.6×) and the fused reducer's
   sliver over `ast-arena`, which LLVM does not reproduce.
2. **Managed runtimes hide layout.** Boxed/GC'd nodes make contiguity
   invisible: `ast-arena` is *slower* than pointer-AST in Python and Haskell,
   and "no tree" shrinks from C++'s ~2.5× to a ~20–30 % edge in Python and
   to nothing in Haskell, where the tree-building `ast-pratt` and
   `ast-recursive-descent` and the no-tree `direct-rd` are within ~1 % of
   each other. That became visible once the Haskell
   registry helpers were marked `INLINE` (2026-09-25): before it, only the
   no-tree forms were specialised to their `Sym` instance and every tree
   builder paid a runtime dictionary call per node.
3. **Top-down multipass loses in every language** — repeated split-scans are
   real extra work no runtime rescues. `multipass-bfs`'s RMQ precompute pays
   off nowhere on random corpora. The bottom-up member is the exception — see
   below.
4. **Overhead compresses the spread:** fastest-to-slowest (max/min of medians)
   is ~4× in C++, ~5× in Rust, ~2.5× in Python and ~2.2× in Haskell — a
   managed runtime's constant overhead narrows the gap between the fastest and
   slowest strategy, it doesn't widen it. (Haskell read ~1.7× until the
   number-parsing fix below removed a constant term from every strategy.)

## Lexing rules — applied to every parser

Every strategy shares one lexer per language so that the tables measure the
parsing approach, not incidental lexing differences. Writing the shortest
correct evaluator (under thirty lines of C++) and then making it fast and
strict surfaced three rules about that lexer — and a rule that helps one
strategy is applied to all of them or to none.

**1. Parse numbers on the fast path.** Number conversion, not whitespace or
dispatch, is the dominant per-leaf cost: the draft went from `strtod` to
`std::from_chars` and got ~40 % faster. The C++ lexer already did this;
Python's `float()` is the same fast path. Haskell's lexer used
`reads :: Double`, which goes through `Rational` — roughly two-thirds of the
fastest Haskell strategies' time. It now takes Clinger's fast path first (a
mantissa of ≤15 digits times an exact power of ten up to 10²² is one
correctly rounded IEEE operation, bit-identical to `reads`; verified on 27
literals including `1e22`, `1e23`, `4.9e-324`, `1e400`), falling back to
`reads` for anything longer. Neutral runner, ns/leaf, median over the four
sizes, median of three CI runs before and after:

| Haskell strategy | before | after | × |
|---|--:|--:|--:|
| ast-recursive-descent | 1 392 | 504 | 2.8 |
| ast-pratt | 1 403 | 480 | 2.9 |
| ast-arena | 1 669 | 686 | 2.4 |
| multipass | 1 888 | 993 | 1.9 |
| multipass-bfs | 2 320 | 1 441 | 1.6 |
| multipass-reverse-fold | 1 702 | 768 | 2.2 |
| direct-recursive-descent | 1 458 | 522 | 2.8 |
| direct-reverse | 1 438 | 536 | 2.7 |
| bytecode-vm | 1 526 | 597 | 2.6 |

Same tiers, 1.6–2.9× less time — the earlier Haskell columns were largely
measuring `reads`. Two things the constant term had hidden: the arena forms'
penalty against the pointer classics is ~1.4–2.9× (not ~1.3×), and the
Haskell spread is ~3×, roughly the same as Python's.

**2. Don't materialise what the algorithm never indexes.** Any extra pass
over the input — even a one-line whitespace strip — costs more than it saves,
and a token array is the biggest such pass. The shared C++ lexer therefore
has two modes over one set of rules: `Lexer::next()` streams one token at a
time, `tokenize()` builds the array by calling it. Every strategy that reads
its input left to right with bounded lookahead streams — the classics,
`ast-arena`, `direct-*`, `bytecode-vm`, and the fused reducer (one token of
lookahead). The divide-and-conquer family and the buffered `multipass-reverse`
keep the array because their algorithms index it — a property of those
algorithms, honestly paid — and for them the token shrank from 24 to 16
bytes. Neutral runner, ns/leaf, median over the four sizes, median of three
CI runs before and after:

| C++ strategy | tokens | before | after | Δ |
|---|---|--:|--:|--:|
| ast-recursive-descent | stream | 179 | 127 | −29 % |
| ast-shunting-yard | stream | 187 | 129 | −31 % |
| ast-pratt | stream | 178 | 130 | −27 % |
| ast-arena | stream | 85 | 72 | −16 % |
| multipass-reverse-fold | stream | 83 | 68 | −18 % |
| direct-recursive-descent | stream | 67 | 50 | −25 % |
| direct-shunting-yard | stream | 74 | 49 | −33 % |
| direct-reverse | stream | 66 | 50 | −25 % |
| bytecode-vm | stream | 86 | 59 | −31 % |
| multipass | array | 235 | 235 | 0 % |
| multipass-arena | array | 135 | 130 | −4 % |
| direct-mp | array | 110 | 107 | −3 % |
| multipass-bfs | array | 150 | 156 | +4 % |
| multipass-reverse | array | 108 | 99 | −8 % |

The token array was 16–33 % of every streaming strategy's time; the 16-byte
token is worth a few per cent at most for the array-bound ones. The ranking
inside each tier is unchanged, but two earlier claims moved: the C++ no-tree
tier is now a three-way tie at ~50 ns/leaf, and the fused reducer's nestchain
lead over `ast-arena` shrank from ~0.6× to ~0.8× because `ast-arena` no
longer pays for a token per parenthesis.

Python keeps its token list: a generator-based stream is a wash there
(0.93–1.05× across six pinned runs) — the interpreter's per-token dispatch
dominates whichever way the tokens arrive. Haskell's token list is lazy and
already fuses with its consumer.

**3. Measure the lexer, don't guess it.** `direct-scannerless` is
`direct-recursive-descent` with the lexer fused into the grammar: each rule
compares the next *character* against the one or two it can accept, and
whitespace is consumed exactly once, right after each token. It obeys the same
lexical rules as the shared lexer and passes the same spec and fuzz suites. It
is not a new parsing algorithm; it is the control row that turns "the lexer
is expensive" into a number per language (ns/leaf, median over sizes and
runs; the ratio's range is over the three runs):

| | `direct-rd` | `direct-scannerless` | control ÷ `direct-rd` |
|---|--:|--:|--:|
| C++ | 41 | 32 | 0.77 (0.77–0.77) |
| Rust | 47 | 29 | 0.62 (0.62–0.63) |
| Python | 2 318 | 1 694 | 0.73 (0.73–0.73) |
| Haskell | 533 | 480 | 0.90 (0.90–0.91) |

(Ratio: median of the three per-run ratios; runs 36226353574 / 36226831278 /
36227295521.) C++ and Python pay roughly a quarter of `direct-rd`'s time
for having a lexer at all, Rust over a third; in Haskell the control buys
~9–10 % — measurable now that round-robin timing has tightened the Haskell
runs, but still the smallest share of the four. The remaining gap is context: after an operand the fused
parser tests only operator characters, where a general lexer classifies the
character first. That is intrinsic to having a lexer, and it is the ceiling
on what rule 2 can recover.

## `multipass-reverse` — the two results

Bottom-up construction: reduce the tightest-binding constructs first, one
in-place sweep per precedence level; no split search, recursion only per paren
group. Same tree, opposite order. **Algorithm walk-through:
[docs/multipass-reverse.md](docs/multipass-reverse.md).**

### Result 1 — vs the classics: competitive — narrowly ahead in C++, a tie in Rust and Python

ns/leaf at n=10000, neutral runner (median of runs 36226353574 /
36226831278 / 36227295521):

| language | `multipass-reverse-fold` | `multipass-reverse` | fastest classic tree builder | fastest overall |
|---|--:|--:|--:|--:|
| C++ | **56** | 81 | ast-arena 58 | direct-sy 40 (direct-reverse, direct-rd within 3 %) |
| Rust | 65 | 90 | ast-arena 65 (tie) | direct-rd 47 (direct-reverse within 1 %) |
| Python | **2 801** | 3 772 | ast-sy 2 946 | direct-reverse 2 159 |
| Haskell | 552 | 824 | **ast-rd 437** | ast-rd 437 (direct-rd 443, direct-reverse 467) |

(The lexer-free control `direct-scannerless` sits at 32 / 30 / 1 695 / 425.)

The fused form is the fastest tree builder in C++ (+1.7…+9.2 % ahead of
`ast-arena`, +2…+4 % from n=100 up, positive in all 12 size×run
measurements across three independent CI runs — a consistent sliver, not a
margin). In Python it is narrowly ahead of the best pointer classic at
n=10, 100 and 10000 (+4…+6 %) and a tie with `ast-pratt` at n=1000
(+0.3…+0.5 %, ~1 % ahead of `ast-rd`). In Rust it is a tie with
`ast-arena`: ahead at n=10 (+3 %), within ±1 % in every run at n=100, 1000
and 10000 — so LLVM does not reproduce GCC's sliver. Its no-tree twin
`direct-reverse` is genuinely faster in Python (+11…+13 % over
`direct-sy`, +7…+26 % over `direct-rd`, in every run at every size), but in
C++ it is a **three-way tie** with `direct-recursive-descent` and
`direct-shunting-yard` at ~41 ns/leaf (+0.5…+4 % vs `direct-rd`, −3…0 % vs
`direct-sy` across sizes and runs), and in Rust it is level with
`direct-rd` (−2…+2 %) and 6–7 % ahead of `direct-sy`. Haskell is the
exception: the pointer classics lead every arena form, from ~1.1–1.2× at
the tight end (`ast-arena`) to ~2–3× at the wide end (`multipass-bfs`); the
fold itself trails `ast-rd` by ~1.1× up to n=1000 and ~1.25× at n=10000
(~1.2× and ~1.4× before the shared arena carrier was made strict — one
change to the carrier all five arena strategies use, the fold's own code
untouched; see the [Haskell README](haskell/README.md#the-arena-carrier)),
and `direct-reverse` trails `direct-rd` by 1–9 % on the random corpus in
every run; on structured shapes it narrowly wins powchain, ties towerchain
and loses sumchain and nestchain by 1–15 % (full scoreboard:
[one-pager](docs/one-pager.md)). The buffered `multipass-reverse` — the
pedagogical version — trails its fused form by ~1.3–1.5×. Design of the fused form:
[docs/multipass-reverse.md](docs/multipass-reverse.md#the-fused-variant).

### Result 2 — vs its family: ahead on every tested input

The top-down variants answer "where is the loosest operator in this range?" by
scanning. Two structured inputs make that quadratic:

1. **powchain** `3^2 * 2^2 / 2^2 * …` (factored monomials) — mixes `^` with
   `* /`, defeating both the flat-chain fold and the prec-1 early exit: every
   split rescans its range. Θ(n²) for `multipass`/`-arena`/`direct-mp`;
   `multipass-bfs`'s O(1) RMQ splits survive.
2. **towerchain** `1^1^…^1 * 1 * …` — the "is this range flat?" check rereads
   the `^` prefix after every split. Θ(n²) *even with O(1) splits* — catches
   `multipass-bfs` too.

`multipass-reverse` never asks the question: **Θ(n) on both.** A
single-precedence control chain (sumchain) keeps the whole family linear,
isolating *mixed precedence* as the trigger; a fourth shape (**nestchain**,
deep parentheses — the one construct where bottom-up recurses) attacks
reverse's own worst case, and it stays roughly flat there too on the run
medians (51–55 ns/leaf across sizes, C++ CI runs 36226353574/36226831278/
36227295521 — within ~7 % from m=512 to m=8192 in each run; `ast-arena`
sits at 34–35 across all three sizes). The fused form is flatter still and
the fastest tree builder on that shape outright (27 vs `ast-arena` 35 at
m=8192, medians across the same three runs).

Pre-fix, powchain (last CI run before the top-down family was patched; sizes
differ per language and the gap grows with size, so ratios are not
cross-language comparable):

| family member | C++ m=8192 | Python m=1024 | Haskell m=4096 |
|---|--:|--:|--:|
| top-down (`multipass` / `-arena` / `direct-mp`) | 3 236–4 864 | 52 368–53 140 | 9 757–10 943 |
| `multipass-bfs` (RMQ) | 84 | 13 709 | 3 010 |
| **`multipass-reverse`** | **43** | **3 135** | **1 302** |

**The fix** (applied to the top-down variants in all four languages): every
linear scan is bounded at 16 candidates; past the budget, per-depth sorted
position arrays per precedence class answer splits by binary search — Θ(n²)
drops to O(n log n). `multipass-arena`/`direct-mp` in C++ additionally use an
AVX2 flat/split check; Python and Rust fold whole precedence levels on budget trip;
Haskell materialises buckets lazily. Effect at m=8192 (ns/leaf, neutral
runner; "after" cells are flat across sizes — no quadratic growth; "after"
is the 2026-09-26 batch):

| | powchain before → after | towerchain after † |
|---|--:|--:|
| `multipass` | 3 791 → **132** | 119 |
| `multipass-arena` | 4 864 → **71** | 84 |
| `direct-mp` | 3 236 → **55** | 53 |
| `multipass-bfs` | 84 → 76 (splits were already O(1)) | **71** |
| `multipass-reverse` (unchanged) | 43 → 37 | 35 |
| `multipass-reverse-fold` | — → **27** | **28** |

† towerchain landed with the fix, so it has no pre-fix CI run (discovery-laptop
pre-fix: ~2 900–11 000 ns/leaf). The Python and Haskell ports close the same
holes the same way; their adversarial chains are flat on the neutral runner too.

So: the rest of the family is linear now — **because it was rescued**, with
budgets, buckets and SIMD, and it still trails bottom-up by 1.5–3.5× (1.9–4.9×
against the fused form, powchain and towerchain). `multipass-reverse` is **the only member of the
family whose worst case is its average case**: same two columns, same code,
no machinery. The pre-fix numbers are preserved from CI and cannot be
regenerated from current sources; the shipped binaries reproduce the "after"
column (`./build/adversarial_bench`, `python3 python/adversarial.py`,
`cabal run adversarial`).

**A runner-variance note on C++ sumchain.** C++ sumchain at m=8192 was
noticeably noisier than every other shape across the three 2026-09-07 runs
(34136843367 / 34137622680 / 34138407724), for several otherwise-unrelated
strategies — and the pattern reproduces the one seen in the *previous*
2026-09-07 batch, just on a different run of the three. `direct-mp` (52.2,
51.7, **64.9**) and `multipass-arena` (44.3, 43.5, **75.2**) both spike on
the same run this time, 34138407724 — consistent with a shared cause, since
both belong to the top-down family's rescue-patch machinery.
`direct-shunting-yard` (35.7, 27.6, 18.3) is also volatile on this shape,
but its own pattern doesn't line up with theirs: it's *high* on the first
run and low on the one where `direct-mp`/`multipass-arena` spike — and it
doesn't share the AVX2 path those two use (checked: `grep -l AVX2
cpp/src/*.cpp` finds only `multipass.cpp`, `multipass_arena.cpp`,
`multipass_lean.cpp`). Meanwhile `direct-recursive-descent` (19.3–23.0) and
`multipass-reverse-fold` (32.4–36.0) stay essentially flat across all three
runs. Two different strategies get shaken by this shape in two different
patterns, on two different runs across two independent batches; the common
thread is the shape and the runner, not a single code path or a one-off
fluke — read it as sumchain-at-m=8192 being a noise-sensitive measurement
point on this CI runner, not a code regression.

One consequence worth flagging: in its low mode (~44), `multipass-arena`
lands within noise of the buffered `multipass-reverse` form (45.4, 44.3,
48.8 across the same three runs) on this same shape — not a rescued-family
win, since sumchain's single precedence level gives the split-scan rescue
machinery nothing to search, so it and the plain buffer end up doing the
same trivial pass. This does not affect the "no worst-case machinery"
argument (that's about `multipass-reverse` vs. its own worst case,
nestchain, where it stays clearly ahead of `multipass-arena`), and the
fused form stays ahead of the whole family on sumchain regardless — by at
least 1.35× at the median in that batch, ~1.1× over `direct-mp` in the
2026-09-25 one and ~1.06–1.08× (+4…+13 % per run) in the two 2026-09-26
ones, after the top-down members stopped paying per-call allocations — in
the latest batch that is *ahead, narrowly* rather than *best*, the only
family cell short of *best*. It widens the C++ sumchain cells in the
[one-pager](docs/one-pager.md)'s Table 2 and Table 3 scoreboards; the
median-based numbers elsewhere in this document already account for it.

The 2026-09-25 batch (36163486136 / 36163570591 / 36163577544) hit the same
shape again, differently: its middle run was slow on sumchain for nearly
every strategy, the fused form included (`multipass-reverse-fold` 16.0 /
32.9 / 22.2, `direct-mp` 19.5 / 52.0 / 24.4, `direct-sy` 11.9 / 28.7 /
16.7; the first run is the faster CPU). Same reading: a noise-sensitive
measurement point on this runner, not a code path. In the 2026-09-26 batch
(one CPU model, round-robin timing) sumchain is calmer but still the
widest C++ cell on the family board: the fold's lead over `direct-mp` was
+13 % on one run and +5…+6 % on the other two, and +4…+8 % in the batch
the tables now use.

Per-language detail: [`cpp/`](cpp/README.md) · [`rust/`](rust/README.md) · [`python/`](python/README.md) · [`haskell/`](haskell/README.md).

## Layout

```
cpp/        C++26 implementation (cpp/README.md — the deep dive)
rust/       Rust implementation (rust/README.md — what the port taught)
haskell/    Haskell implementation
python/     Python implementation
bench/      shared corpus generator + (generated) corpus/
docs/       multipass-reverse.md (algorithm), one-pager.md (verdict)
```
